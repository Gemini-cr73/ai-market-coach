name: Keep AI Market Coach Awake

on:
    schedule:
        - cron: "20 */6 * * *"
    workflow_dispatch:

jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - name: Ping AI Market Coach
              run: |
                  curl -L --max-time 60 https://YOUR-AI-MARKET-COACH-URL.streamlit.app || true
